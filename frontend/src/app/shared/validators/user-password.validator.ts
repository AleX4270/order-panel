import { AbstractControl, FormGroup, ValidationErrors, ValidatorFn } from "@angular/forms";

export function validateUserPassword(): ValidatorFn {
    // return (form: AbstractControl): ValidationErrors | null => {
       
    // }
}